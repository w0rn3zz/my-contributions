# Чек-лист демонстрации MVP

## Подготовка

- [x] Чистая временная БД проходит миграции `000001`–`000007` и roundtrip `up → down → up` (проверено 2026-08-09: 12 опубликованных Тем после повторного up).
- [x] `cd backend && go test ./...` проходит полностью (2026-08-09; дополнительно `-race` для HTTP concurrency и limiter tests).
- [ ] `make build-ollama && make up-ollama && make ollama-init` запускает PostgreSQL, API, gateway и qwen3:8b.
- [ ] Production-сборка веб-клиента включена в образ gateway и открывается на `http://localhost:3000`.
- [ ] `ngrok http 3000` открывает тот же интерфейс по HTTPS; запросы идут на same-origin `/api`.
- [ ] Сделана локальная запись экрана как резерв на случай сбоя сети.

## Пользовательский путь

- [ ] Зарегистрировать Пользователя, выйти, войти снова; Логин и Прогресс сохраняются.
- [ ] Для покупателя пройти Уровни 1–2 готовыми вариантами, Уровень 3 обоими видами ответа и Уровень 4 свободным текстом.
- [ ] Повторить тот же путь для продавца; убедиться, что ролевые ветки независимы.
- [ ] Проверить закрытый Уровень (`403`) и открытие следующего после одной Звезды.
- [ ] На Уровне 4 проверить отказ `finish=true` до третьего ответа, досрочный итог на третьем и автоматический итог на пятом.
- [ ] Запустить Свободную игру для обеих ролей; тип Виртуального собеседника не виден до итогового разбора и не влияет на Прогресс Уровней.
- [ ] Отключить Ollama перед отправкой ответа: UI показывает повторяемую ошибку, после восстановления тот же ответ принимается один раз.
- [ ] Проверить экраны регистрации, выбора роли, карты Уровней, чата, итога и ошибки AI.

## Возможности Issue #68

- [ ] Через административный API создать полную Тему, Теорию и Quiz; увидеть `409` для неполной публикации и затем успешно опубликовать агрегат со Сценариями Уровней 1–4.
- [ ] Убедиться, что пользователь не видит draft/archive и `is_correct`, а архивная Тема сохраняет исторические Прогресс, Quiz attempts, Прохождения и Results.
- [ ] Проверить стабильность Задания дня после refresh, независимость ролей, московскую полночь и автоматическое выполнение Теорией, успешным Quiz, Result со Звездой и Свободной игрой.
- [ ] Достичь auth- и AI-квот контролируемыми запросами: UI получает `429 RATE_LIMITED` и `Retry-After`, а лишняя учётная запись или переход Прохождения не появляется.
- [ ] Отправить два параллельных AI-запроса одного Пользователя и подтвердить один provider call; запросы разных Пользователей остаются независимыми.

Автоматически выполнено 2026-08-09: чистый PostgreSQL migration roundtrip; populated-schema roundtrip с сохранением Пользователя, Прогресса, Quiz attempt, Прохождения и Result; PostgreSQL content acceptance; полный Go suite и race-проверка конкурентных seams. Frontend в этой рабочей копии не поставляется (`frontend/package.json` отсутствует), а живой Ollama/qwen3:8b и браузерный путь остаются пунктами общей репетиции #47.
